# EXERCISE

## 1

- 30

## 2

- 버그
  - 일어날 수 없다고 가정한 상황
  - 코드 수정이 필요함
- 오류
  - 실행 중 일어날 수 있는 예측 가능한 상황
  - 프로그램이 대처

## 3

- CPU 내부의 저장 공간
- 가장 빠르게 접근할 수 있음

## 4

- a: 컴파일 오류
- b: register 키워드가 붙은 변수는 주소의 개념이 없기 때문에 포인터 변수에 &로 주소값을 얻어 저장할 수 없다. 따라서 p에 num의 주소값을 저장하고 싶다면, register 키워드를 제거하면 됨

## 5

- init_array() 함수 내부에서 선언한 배열 x의 수명은 init_array()함수가 리턴될 때 끝난다. 따라서 main() 함수에서 변수x에 init_array()의 반환값을 저장한 뒤 변수x에 접근하면 유효하지 않은 메모리에 접근하는 것이 된다.
  - dangling pointer
  - memory stomp
- 배열의 수명을 프로그래머가 관리하기 위해서는 동적할당으로 힙 메모리를 할당받아서 반환하도록 변경하면 된다.

## 6

```c++
0
11
12
13
14

```

## 7

- a: 예
- b: free(a)를 호출하기 전 *a, *(a+1), *(a+2)에서 할당한 메모리를 해제한다.

## 8

- a: 예
- b: realloc에서 기존의 sentence의 값을 그대로 반환하지 않을 수 있다. 또는 realloc에서 할당에 실패할 수도 있다. 따라서 임시 const char* 변수를 선언헤 realloc의 반환값을 대입하고 NULL인지 확인 후 sentence에 대입해야한다.

## 9

- a: 문제없음
- c: 아래 와 같음
```
x: 12
y: 6

```

## 10

- 규칙이 다름
- 스택: 후입선출
- 큐: 선입선출

## 11

- a: 40, 15
- b: 10, 10
- c: 30, -1

## 12

- a: 컴파일 오류
- b: #define other_type_t my_type_t로 변경, 식별자를 대체_목록으로 변경

```
10
```

## 13

```c++
-5
```

## 14

- a: 예
- b: multiplier.c 를 선언한다. / #include "multiplier.h"로 헤더 파일 인클루드 / extern inline multiply(int x, int y)로 다시 선언하기

## 15

- 포인터 변수에 붙이는 키워드로, 컴파일러에게 메모리가 겹치지 않는다고 힌트를 줘 컴파일러의 방어적인 구현을 막을 수 있다. 성능에 도움이된다.
