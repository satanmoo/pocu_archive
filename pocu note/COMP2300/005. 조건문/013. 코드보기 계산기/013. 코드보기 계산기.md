---
tags:
  - COMP2300_week_4
  - COMP2300
---
```asm
    .PROCESSOR 6502
    .ORG $8000

num0=$1000
num1=$1001
op=$1002
out=$10
    ldx #$FF
    txs

    ; == DEFINE INPUTS HERE ===
    lda #12
    sta num0

    lda #16
    sta num1

    lda #'+
    sta op
    ; =========================

    lda num0 ; A = solution
    ldx op

    cpx #'+
    bne sub

    clc
    adc num1
    jmp end

sub:
    cpx #'-
    bne lshift

    sec
    sbc num1
    jmp end

lshift:
    cpx #'<
    bne rshift

    ldy num1

lloop:
    beq end
    asl
    dey
    jmp lloop

rshift:
    cpx #'>
    bne end

    ldy num1

rloop:
    beq end
    lsr
    dey
    jmp rloop

end:
    sta out

    .ORG $FFFC,0
    .WORD $8000
    .WORD $0000
```

![[Pasted image 20260211013912.png]]

![[Pasted image 20260211013932.png]]

![[Pasted image 20260211013943.png]]

![[Pasted image 20260211013953.png]]

![[Pasted image 20260211014003.png]]

> ldx op

![[Pasted image 20260211014010.png]]

*X 레지스터* 의 값에 따라 분기를 하겠다. 예상할 수 있다.
- [[024. X, Y 색인 레지스터]] 참고

![[Pasted image 20260211014127.png]]

> cpx #'+
> bne sub

![[Pasted image 20260211014151.png]]

상수표기법으로 *아스키 +* 을 표현했다.
- [[021. 상수, 포인터 변수]] 참고

*X 레지스터* 의 값(op)와 비교한다.

`bne` 를 사용하는 의도는 피연산자('+')와 같지 않으면 분기한다.
- sub 블록으로 이동

![[Pasted image 20260211014703.png]]

op 값이 '+' 인 경우 더하기를 수행한다.

![[Pasted image 20260211014757.png]]

> [!INFO] 분기 로직은 동일하다.

>  sec
>  sbc num1
>  jmp end

op 값이 '-'인 경우 빼기를 수행한다.
- 캐리 플래그 1로 설정
- 이미 *A 레지스터* 에는 *num0* 이라 `sbc`를 사용한다.
- [[010. 8비트 뺄셈#sbc]] 참고

![[Pasted image 20260211020134.png]]

![[Pasted image 20260211020205.png]]

> 	ldy num1
> lloop:
> 	beq end
> 	asl
> 	dsy
> 	jmp lloop

![[Pasted image 20260211020320.png]]

*Y 레지스터* 에 num1 값을 로드한다.
- 반복 횟수

종료조건은 *Z 레지스터* 가 1인 경우

`asl` 로 왼쪽 비트 쉬프트

`dey` 로 반복 횟수 감소
- *Y 레지스터* 의 값이 0이 되면 제로 플레그가 켜진다.

> [!INFO] Right Shift의 논리는 동일하다.





