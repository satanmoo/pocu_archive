---
tags:
  - COMP2300_week_4
  - COMP2300
---
 ## 2배 정밀도 덧셈 예

*2배 정밀도 덧셈 *은 [[002. 최상위 비트 받아올림의 처리#최상위 비트 받아올림의 처리]] 에서 언급한 두 가지 방법 중 하나이다.
- *오버플로* 방식과 차별점은 값을 잃지 않는다는 것이다.
- *Carry Bit* 를 사용하잖아!

![[Pasted image 20260128010235.png]]

### 코드를 한 줄 씩 이해해보자.

```asm
NUM0=%01101000
NUM1=%10011010
```

[[021. 상수, 포인터 변수#상수 - 상수형 변수]]] 에서 설명한 것 처럼 *상수형 변수*를 사용할 수 있는 "주소 지정 모드"는 *즉시 모드* 뿐이다.
- 다른 "주소지정 모드"를 사용할 수 있다. 하지만 *상수형 변수*를 사용할 수 없겠죠?

```asm
out1 = $2000
outh = out1 + 1
```

[[021. 상수, 포인터 변수#변수명]] 에서 설명한 *포인터 변수*를 사용했다. 메모리의 주소에 이름을 붙이는 개념이다.

`outh = $2001` 과 동일하다.

```asm
ldx #$FF
```

`ldx`: *X 레지스터*에 값을 로드하는 명령이다. *즉시 모드*로 *X 레지스터* 에 값 `0xFF` 이 저장된다.

`0xFF` 는 특별한 값이다. [[015. 6502의 메모리 페이지 레이아웃]] 에서 설명한 *스택 메모리* 페이지에 속하는 주소 중 최대값이다.
- 스택의 시작 지점을 가리킨다.
	- [[025. S 레지스터#색인 레지스터 - S]] 에서 스택의 시작 지점이 `0xFF` 고 "push" 하면 값이 1 줄어든다고 설명했다.

```asm
txs
```

*Transfer X To Stack Pointer* 
- [[024. X, Y 색인 레지스터]] 참고

현재 *X 레지스터* 의 값은 `0xFF` 다. 즉 스택의 시작 주소이다. 이 값을 *스택 포인터*에 복사한다.

```asm
ldx #$FF
txs
```

이 두 줄은 스택 초기화의 정석 패턴이다.

```asm
clc
```

캐리 플래그를 0으로 초기화

다음에 `adc` 할 때 순수한 덧셈이 되도록 보장한다.

>  A + M + C -> A, C
>    
>    [ADC]

```asm
lda #NUM0
```

*A 레지스터(누산기)* 에 *즉시 모드* 로 값 `0b01101000`를 로드한다.
- 연산의 결과가 반복적으로 누적되기 때문에 누산기라고 부른다.

덧셈 연산의 피연산자를 먼저 누산기에 로드하는 개념이다.

```asm
adc #NUM1
```

피연산자 `0b10011010` 와 이미 누산기에 로드된 값 `0b01101000` 그리고 *캐리 비트* 까지 더한다.
- *캐리 비트* 는 `clc` 로 0으로 초기화 해놨지

덧셈의 결과는 `0b00000010`다. 이 값은 누산기에 저장된다.

8비트를 초과하기 때문에 *캐리 비트*의 값은 1이 된다.

```asm
sta outl
```

여기서 사용된 주소 지정 모드는 *절대 모드* 이다.

우선 `#`이 안 붙어 있기 때문에 *즉시 모드*는 아니다.
- `sta`는 *즉시 모드* 를 지원하지 않는다.

`outl` 의 값은 `$2000`다. 여기서 중요한 것은 값이 2비트 라는 것이다.

`sta` 뒤에 2비트 주소가 오면 *절대 모드*이다.
- 1비트 주소가 오면 *Zero Page*

따라서 2비트 주소 `0x2000` 에 누산기의 값(`0b00000010`)을 저장한다.

```asm
lda #0
```

누산기에 값 0을 로드한다. 누산기 값이 초기화 되었다.
- *즉시 모드*

`sta` 로 메모리에 누산기의 값을 저장했으니 클리어 하는 개념이다.

```asm
adc #0
```

*즉시 모드*

피연산자(`0b00`) 과 기존의 누산기의 값(`0b00`) 그리고 캐리 비트(`0b01`) 까지 더하고 누산기에 저장한다.
- `0b01` 이 누산기에 저장된다.

두번째 바이트(byte 1)의 값이다.
- 자릿수가 올라가는 개념

*캐리 비트* 의 값은 0이 된다.

```asm
sta outh
```

누산기의 값(`0b01`)을 `0x2001` 주소에 저장한다.
- *절대 모드*

리틀 엔디언에 따라 높은 주소에 높은 자리수(상위 바이트)의 값이 저장되었다.

> 두 수는 매직 넘버
> - 다른 주소지정 모드를 사용해도 됨

여기서 "두 수"는 덧셈의 피연산자를 의미한다.

*매직 넘버* 를 사용하기 때문에 *즉시 모드* 가 강제된다. 이 때문에 "다른 주소지정 모드를 사용해도 됨" 라고 표현했다.

> 결과는 2 바이트로 저장 (리틀 엔디언)

`outl`, `outh` 에 저장한다. 리틀 엔디언이라 `outh`에 높으 자리수의 값이 저장된다.

> byte 1 은 원래 없었으므로 0과 더함

"byte 1"은 두번째 바이트를 의미한다.
- 녹색 하이라이트

### `clc`

![[Pasted image 20260130001521.png]]

### `lda #NUM0`

![[Pasted image 20260130001543.png]]

### `adc #NUM1`

![[Pasted image 20260130001607.png]]

### `sta outh`

![[Pasted image 20260130001708.png]]

### `lda #0`

![[Pasted image 20260130001726.png]]

### `adc #0`

![[Pasted image 20260130001740.png]]

받아올림이 발생하지 않았기 때문에 *캐리 비트* 의 값은 0이 된다.

### `sta outh`

![[Pasted image 20260130001903.png]]

### 두번째 `adc` 전에는 *캐리 비트* 를 클리어하지 않았다.

![[Pasted image 20260130001958.png]]

두번째 `adc` 할 때는 받아올림을 해야하니까 당연히 클리어하면 안 된다.

## adc 와 clc

![[Pasted image 20260130002105.png]]

> 두 수를 더하기 전에 받아올림 플래그 (C)를 지워줘야 함
> - ALU는 우리가 수를 더하는 중인지 새로 덧셈을 시작하는 지 모름

새로 덧셈을 시작하는 경우 의도에 맞게 더하기 위해 `clc`가 필요하다.

> adc는 언제나 C값을 같이 더함

니모닉 스펙이 이렇게 정의되어 있다.

> ALU는 adc 연산 결과에 따라 C 비트를 변경

니모닉 스펙이 이렇게 정의되어 있다.

ALU가 해주는 구나?!

> 따라서 첫 바이트를 더하기 전에만 clc를 호출해 0이 더해지게 함

지금까지 본 내용의 결론, 정리, 지침

## clc

![[Pasted image 20260130002449.png]]

회색으로 그레이 아웃된 *N*, *V*, *Z* 플래그는 변하지 않는다.

*C* 플래그만 변한다.
- 반드시 0이된다.

![[Pasted image 20260130002634.png]]

CLC는 *암시 모드* 밖에 없다.
- 피연산자가 없어

## adc

![[Pasted image 20260130002750.png]]

지금 "Work In Progress" 상태다. 수정이 필요하다.

![[Pasted image 20260130002816.png]]

> 두 8 비트 정수 간 덧셈을 수행

덧셈의 대상은 *A 레지스터* 와 *메모리의 값*

> 결과: A 레지스터에 다시 대입
> - bit 7에서 받아올림이 생기면 C 플래그도 켜짐

덧셈의 결과는 *A 레지스터* 에 대입된다.

받아 올림이 발생하면 *C 플래그* 가 바뀐다.

스크린샷을 보면 `adc` 는 *N*, *V*, *Z*, *C* 플래그를 모두 바꾼다?!
- *C 플래그*를 제외한 나머진 조금 있다가 보자.






