
# Assignment 2

## Design

### 클래스 계층

- Product
  - ApertureEnabledProduct
    - Banner
    - Business Card
  - Stamp
  - ApertureEnabledProduct

### 스탬프

상태:

- 색
  - 색을 표현하는 클래스
  - ENUM
- 크기
  - ENUM
  - 규격이 정해진 크기
  - 가로, 세로의 길이를 입력받아서 완전히 커스터마이징 X
  - 3주차 강의, 분무기 예시 참고
- 가격
  - 규격에 따라 결정되는 값
- 문구
  - 고객의 입력에 따라 커스터마이징 하는 값
- 결제방법
  - ENUM
  - 2가지

동작:

### 달력

상태:

- 달력의 종류
  - ENUM
- 크기
  - 달력의 종류에 따라 결정
- 가격
  - 달력의 종류에 따라 결정
- 색
  - 고정으로 흰색
- 출력 방향
  - ENUM
- 결제방법
  - ENUM
  - 2가지

동작:

### 배너

상태:

- 배너 종류
  - ENUM
- 크기
  - ENUM
  - 규격이 정해진 크기
- 가격
  - 크기에 따라 결정
- 출력 방향
  - ENUM
- 색상
  - RGM 자유선택
  - ENUM 아니고, 완전히 커스터마이징하는 입력값
- 문구
  - 컬렉션으로 관리
  - 위치 상태를 가짐(유효한 위치 계산)
  - 크기 상태를 가짐
  - String
- 사진
  - 컬렉션으로 관리
  - 위치 상태를 가짐(유효한 위치 계산)
  - 크기 상태를 가짐
  - 웹서버에 URL로 저장, String
- 결제방법
  - ENUM
  - 2가지

### 명함

상태:

- 명함 종류
  - ENUM
- 단면/양면
  - boolean
  - ENUM
- 가격
  - 단면/양면 여부에 결정
- 크기
  - 고정값
- 출력방향
  - ENUM
- 종이색
  - ENUM
  - 규격이 정해진 색을 표현하는 클래스
- 문구
  - 컬렉션으로 관리
  - 위치 상태 가짐(유효한 위치 계산)
  - 크기 상태를 가짐
    - String
- 사진
  - 컬렉션으로 관리
  - 위치 상태를 가짐(유효한 위치 계산)
  - 크기 상태를 가짐
  - 웹서버에 URL로 저장, String
- 결제방법
  - ENUM
  - 2가지

### 장바구니

상태:

- 물건 컨테이너

동작:

- 물건 제거
- 물건 추가
- 총 가격 계산

## 주의할점

- 출력소 직원들이 정확히 제품을 찍으려면 크기, 재질, 색상, 문구, 이미지 경로 등 필요한 정보들을 읽을 수 있어야 한다는군요.
  - public getter 설계 주의
- 그렇다고 클래스 설계의 추상화/일반화를 망가뜨리지 마세요. 출력소의 시스템은 필요에 따라 다운 캐스팅(downcast, 자식 클래스로 명시적으로 캐스팅함)을 한다고 합니다. 따라서 부모 클래스가 자식 클래스에
  대해 너무 많이 알게 만들지 마세요.
  - 부모 클래스 / 자식 클래스 분리 설계 주의

## ETC

### Java enum class

- 멀티턴 패턴
  - 고정된 수의 인스턴스

## 테스트 실패 해결

- 특정 상품에만 사용되는 ENUM은 특정 상품의 상태로 반드시 가지도록 수정

- Y05_UniformRepresentation
  - ApertureEnabledProduct에서 Text, Image 따로 컨테이너로 관리 -> 공통으로 관리하게 수정

- X04_CartCanReadData
  - ShoppingCart에 컨테이너 getter 추가

- not found: display name
  - 최상위 클래스 Product에 displayName 멤버 변수 추가
  - 자식 클래스에서 private static 메소드로 displayName 값을 결정해서 super 생성자의 매개변수로 넘기기

- 자식 클래스의 전용 상태를 자식 클래스에 가지고 있어야하는가?
  - 이 때 상태는 일반화되서 부모 클래스에 저장됨
  - 장점: 캡슐화, 확장성(전용 상태를 사용하는 경우 쉽게 사용할 수 있음)
  - 단점: 코드 중복 늘어남

- 명함 종류, (D02_CanReadData 등등)
  - not found: size
  - 명함 크기 상태를 non-static으로 개체가 가지도록 수정
