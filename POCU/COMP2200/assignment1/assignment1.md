# Assignment 1

## 파일 스코프의 전역 변수 사용하기

- 이 파일 안에서만 사용하는 전역변수를 선언하려면, static 키워드를 사용해 전역변수를 선언하자

## 열거형 사용하기

- 흑: 0
- 백: 1

## init_game() 함수를 구현한다

- 보드는 초기 크기인 15 x 15로 초기화
    - 2차원 배열을 사용
    - 보드는 게임 실행 중 계속 상태를 유지, 보드의 원소값은 변할 수 있고, 파일 내부 다른 함수에서 접근할 수 있어야함!
- 보드를 초기화할 때 어떻게 구현하는게 편할까?
    - 최대 크기로 2차원 배열을 선언하고, 크기를 나타내는 변수를 활용하는 방식이 좋음
    - 굳이 15 크기로 딱맞게 선언하고 보드의 크기가 변할 때 마다 배열을 복사하고 그럴 필요가 없습니다. 안 쓰는 공간을 의미하는 값을 설정하면 됨!

- 현재 보드의 크기를 의미하는 변수 s_row_size, s_col_size
    - 자료형:
        - unsigned int
        - 과제 명세에서 get_row_count(), get_column_count() 함수의 반환 타입이 unsigned int
    - 보드의 크기는 변할 수 있으면서, 프로그램 실행 중 상태가 유지되어야하고, 파일 내부 다른 함수에서 접근할 수 있어야함!

- 각 플레이어의 점수를 의미하는 변수 s_black_score, s_white_score
    - 자료형:
        - int
        - 과제 명세의 get_score() 함수의 반환 타입에서 힌트를 얻을 수 있음
    - 보드, 보드 크기와 똑같이 성질을 만족해야함

- 어떤 성질을 가진 것들을 전역 변수로 구현할 것인가?

## get_score() 함수를 구현한다

- 각 플레이어의 점수를 전역 변수로 관리하기 때문에 O(1) 시간복잡도로 반환 할 수 있음
- 분기처리
    - switch - case
    - if
    - 알아서 하쇼

## get_color() 함수를 구현한다

- 전역 변수 보드는 2차원 배열인데, 인덱스로 참조하면 됨

## is_placeable() 함수를 구현한다

- 보드 크기를 전역변수로 관리하기 때문에 범위 시간 복잡도 O(1)로 구할 수 있음
    - 보드 크기로 유효한 위치인지(보드 안에 있는지) 확인하는 조건문 쉽게 만들 수 있음

- s_board의 값이 초기상태(-1)일 때만 돌을 놓을 수 있음. 이미 돌이 있으면 못 놓음
    - 보드를 선언할 때 빈 곳의 값은 -1로 초기화

## place_stone() 함수를 구현한다

- color 값 유효성 확인
    - C 의 열거형은 사실상 int라서 내가 열거형으로 선언한 값 말고도 다른 값이 들어올 수 있기 때문에 유효성 검사를 해야함;;
    - 유효하지 않으면 return false

- is_placeable을 활용해서 놓을 수 있는지 확인
    - 유효하지 않으면 return false

- s_board에 돌 놓기
- [점수 갱신]
- return TRUE;

## insert_row() 함수를 구현한다, insert_column() 함수를 구현한다

- 삽입할 수 있는지 확인
    - 유효한 색인지 확인
    - 최대 격자 크기(상수)보다 작아야함
    - 삽입할 위치가 유효한 범위인지 확인
        - s_row_size, s_col_size 에 추가할 수 있음(append)
    - 요구 점수(비용) 이상
- 삽입하고 미는 과정 구현
    - 밀기
    - 점수 감소
        - 돌 색깔에 맞게
    - 행, 열 크기 상태 수정

- 열은 방향만 바뀌고 논리 동일

## remove_row() 함수를 구현한다, remove_column() 함수를 구현한다

- 제거할 수 있는지 확인
    - 유효한 색인지 확인
    - 최소 격자 크기(상수)보다 커야함
    - 삽입할 위치가 유효한 범위인지 확인
        - s_row_size, s_col_size 는 유효한 범위 아니죠?
    - 요구 점수 이상
- 제거하고 당기는 과정 구현
    - 당기기
    - 점수 감소
        - 돌 색깔에 맞게
    - 행, 열 크기 상태 수정

## swap_rows() 함수를 구현한다, swap_columns() 함수를 구현한다

- 스왑할 수 있는지 확인
    - 유효한 색인지 확인
    - 삽입할 위치가 유효한 범위인지 확인
    - 요구 점수 이상

## copy_row() 함수를 구현한다, copy_column() 함수를 구현한다

- 복사할 수 있는지 확인
    - 유효한 색인지 확인
    - 복사할 위치가 유효한 범위인지 확인
    - 요구 점수 이상

## 점수 계산 알고리즘

- 돌을 놓고, 지금 놓은 가장 최근의 돌을 기준으로 추가되는 점수를 계산해야함
    - 직관적으로 생각하면 당연하죠?

- 수강 중 대각선 점수를 계산하는 알고리즘:
    - 장점: 미리 얼마나 몇 번 이동하는지 계산, 오목(이 문제)에 특수하게 사용할 수 있는 방법
    - 단점: 가독성
- 일단 이동하고, 범위 확인하는 방법도 있음:
    - 장점: 익숙한 방식(이동하고 범위 확인하고)으로 오목 규칙이 아닐 때 일반화 가능

- 5개의 돌을 연결하면 +1 점, 6개의 돌을 연결하면 +2 점, 7개의 돌을 연결하면 +3 점 ...

## 경험한 실수

```c++
for (i = s_row_size - 2; i >= row; --i) {
        for (j = 0; j < s_col_size; ++j) {
            s_board[i + 1][j] = s_board[i][j];
        }
    }
```

- row가 0 인 경우 문제점
- row, i가 size_t라서 --i경우 엄청 큰 수가 되어. 반복문을 다시 돌게 된다.
- 결국 반복문에서 사용하는 변수들을 감소하는 경우 어떤 수가 될 지 생각을 하고 음수가 될 가능성이 있으면 int로

```c++
/* dst: s_board[s_row_size-1], src: s_board[s_row_size-2] ... */
for (i = (int)s_row_size - 2; i >= (int)row; --i) {
for (j = 0; j < s_col_size; ++j) {
s_board[i + 1][j] = s_board[i][j];
}
}
```

- i >= (int)row로 바꾸자.
    - i >= row의 경우 int 와 unsigned int를 비교할 때 unsigned int로 비교하게 된다.

- 루프 시작과 끝을 나타내는 변수를 따로 선언하는게 더 좋아보임!

## 리팩토링

- 핼퍼 함수 사용
- 변수 이름, 함수 분리
- 격자의 인덱스를 표현하는 자료형은 int 로 구현하자
    - 유효하지 않은 좌표 -1을 포함하는 개념으로
    - 인덱스 감소처리에서 0 경계에서 오버플로우 나는거 방지
