# Assignment 1

## 공통 요구 사항 분석

- 특수 기술 때문에 보드의 크기가 바뀔 수 있습니다. 보드의 최소 크기는 10 x 10이며, 최대 크기는 20 x 20입니다. 보드의 크기가 10 x 10이 되면 특수 기술을 사용해도 보드가 더 이상 줄어들지 않습니다. 마찬가지로 20 x 20이 된 보드는 특수 기술 때문에 크기가 증가하지 않습니다.
  - 보드 크기의 최소, 최대가 존재한다.
    - 1. 메크로로 헤더 파일에 최소, 최대를 선언하자.
    - 2. 전역변수에 const키워드를 붙여서 선언하자.
      - 전역 변수를 외부에서 사용하게 할 것인가? 아니면 static으로 선언해서 내부에서만 사용하게 할 것인가?
      - 요구사항을 봤을 때 내부에서만 사용하는게 옳다. getter가 있거든..

- 한 플레이어는 검은색 돌을, 다른 플레이어는 하얀색 돌을 사용합니다.
  - 돌의 색깔은 검은색, 하얀색 두 가지가 존재한다. 따라서 열거형으로 주어졌음
  - 흑은 0, 백은 1로 정의하자.
  - 따라서 board는 정수형 배열(?) 선언하자.
    - 1. 2차원 배열로 처음부터 최대 크기로 선언하고, 배열 안의 값을 사용 안 하면 되잖아? (스택에 저장되는 방식, 선언 시 크기 고정)

### 2차원 배열 초기화

- 그냥 속 편하게 2중 for문으로 합시다.

- 플레이어는 순서를 바꿔가며 자신의 돌을 하나씩 보드 위의 빈 칸에 올려놓습니다. 이 게임의 목표는 가로, 세로, 또는 대각선으로 끊기지 않게 5개의 돌을 연결하는 것입니다. 플레이어들은 끊기지 않고 5개 이상의 돌을 연결시키면 점수를 획득합니다.
  - 5개의 돌을 연결하면 +1 점
  - 6개의 돌을 연결하면 +2 점
  - 7개의 돌을 연결하면 +3 점
  - 등...
  - 점수 계산을 어떻게 할 것인가?
    - 돌을 놓을 때 마다 계산해서 변수에 저장?
    - 특수 기술을 사용할 때마다 계산해서 변수에 저장?
    - 진행과정에서, 이벤트(돌 놓거나, 기술 사용, 즉 턴)마다 계산해서 변수에 저장하자.
      - 돌을 놓을 때 점수 계산하는 방법
        - ... 이게 어려운데?
        
        


## init_game() 요구사항 분석

- init_game() 함수는 인자를 받지 않습니다.
- 이 함수는 게임을 초기화합니다.
- 보드는 초기 크기인 15 x 15로 초기화됩니다.
  - 2차원 배열을 사용하자.
  - 보드는 게임 실행 중 계속 상태를 유지해야함.
  - 보드의 크기가 정사각형이 아님, 가로, 세로 크기가 다를 수 있음.
  - 어렵게 가지말고, 20*20 2차원 배열로 선언해서, 15*15만 사용하자.
    - s_row, s_col 은 모두 unsigned int로 주어진 시그니처 get_row_count();, get_column_count();에서 반환값이 모두 unsigned int거든
- 모든 플레이어의 점수는 0으로 리셋됩니다.
  - 점수 또한 게임 실행 중 계속 상태를 유지해야함.
  - static 변수로 선언하자.
  - 자료형은 unsigned int 점수가 0보다 작을 수는 없으니까?
    - 근데 get_score 함수 요구사항을 보니 int로하는게 맞는듯 ㅇㅇ;
  - 최대 점수가 몇 일까? 20 * 20에서 쭉 연결하면 20점이네... 너무 여유로운데?

## int get_score(const color_t color) 요구사항 분석
- switch case 사용하면 됨
- [새로운 사실] enum에서도 switch case를 사용할 수 있다. enum은 int에 별명을 지은 것 뿐이다.

## 2.6 get_color() 함수를 구현한다. 요구사항 분석
- s_board의 인덱스로 값을 참조해서 반환하면 된다.
- s_board를 초기화할 때 -1로 초기화 하자. 즉 쓰지 않는 값은 -1로 앞으로 정한다.
- [새로운 사실] 2차원 배열을 어떻게 초기화하지?

## 2.7 is_placeable() 함수를 구현한다. 요구사항 분석

- s_board에서 사용 중인 범위를 벗어나는지 확인하면 된다.
- 우리는 s_col, s_row의 크기를 알고 있잖아? 그러니까 그 범위를 넘어가면 안된다.
- 그리고 s_board의 값이 -1이 아니면 안된다. 이미 돌이 있으면 못 놓기 때문이다.

## 2.8 place_stone() 함수를 구현한다

- is_placeable을 활용해서 놓을 수 있는지 확인
- color 값 switch-case로 유효한지 확인
- s_board에 돌을 놓는다. 그리고 return TRUE;

## insert_row() 함수를 구현한다, insert_column() 함수를 구현한다

- 배열의 삽입, 삭제가 자주 발생한다. column, row가 자주변하게 되있음
- 처음에 가정했던 자료구조를 변경해야하는 건가.
  - 고정 2차원 배열에서 동적 2차원 배열로 변경해야하는 건가?
  - 생각해보니까 어차피 똑같음. 행, 열이 둘다 변하기 때문에 오히려 더 복잡해짐
  - 그냥 2차원 배열로 하자.
    - 2트째 도전해볼 것 

## remove_row() 함수를 구현한다, remove_column() 함수를 구현한다

- 삽입이랑 다른점은 삭제 후 빈 공간을 채워야한다는 것

## 배울점1

```c++
for (i = s_row_size - 2; i >= row; --i) {
        for (j = 0; j < s_col_size; ++j) {
            s_board[i + 1][j] = s_board[i][j];
        }
    }
```

- row가 0 인 경우 문제점
- row, i가 size_t라서 --i경우 엄청 큰 수가 되어. 반복문을 다시 돌게 된다.
- 결국 반복문에서 사용하는 변수들을 모두 int로 바꾸자.

```c++
/* dst: s_board[s_row_size-1], src: s_board[s_row_size-2] ... */
    for (i = (int)s_row_size - 2; i >= (int)row; --i) {
        for (j = 0; j < s_col_size; ++j) {
            s_board[i + 1][j] = s_board[i][j];
        }
    }
```

- i >= (int)row로 바꾸자.
  - i >= row의 경우 int 와 unsigned int를 비교할 때 unsigned int로 비교하게 된다.

